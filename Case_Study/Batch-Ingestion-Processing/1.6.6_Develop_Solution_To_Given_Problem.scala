6>	How well does weather predict plane delays?

Weather has no influence on flight if : (([avg_absolute_delay_with_weather_delay - avg_absolute_delay_without_weather_delay]) - ([avg_absolute_weather_delay])) === 0
Weather is increasing the flight delay if : (([avg_absolute_delay_with_weather_delay - avg_absolute_delay_without_weather_delay]) - ([avg_absolute_weather_delay])) >== 0
Weather is decreasing the flight delay if : (([avg_absolute_delay_with_weather_delay - avg_absolute_delay_without_weather_delay]) - ([avg_absolute_weather_delay])) <== 0

Note : Here we are considering ideal scenario i.e. wind is neither increasing the speed of plane (i.e. upStream) nor decreasing speed of plane (i.e. downStream)
=====================================================================================================================================================================
                                ##### #   #  #
                                #     ##  #  # #
                                ###   # # #  #  #
                                #     #  ##  # #
                                ##### #   #  #
=====================================================================================================================================================================
